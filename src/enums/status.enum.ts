export enum StatusActivation {
  Pending = "pending",
  Active = "active",
  Inactive = "inactive",
  Cancelled = "cancelled",
}

export enum StatusTransaction {
  Pending = "pending",
  Paid = "paid",
  Delivered = "delivered",
  Cancelled = "cancelled",
}

export enum StatusCommand {
  Pending = "pending",
  Completed = "completed",
  Closed = "closed",
  Cancelled = "cancelled",
}

export enum StatusDelivery {
  Pending = "pending",
  InProgress = "in_progress",
  Completed = "completed",
  Closed = "closed",
  Cancelled = "cancelled",
}